<%= form_for(comment) do |f| %>
  <%= f.label :body %>
  <%= f.text_area :body %><br>

  <%= f.hidden_field :article_id, :value => params["article_id"] %>

  <%= f.submit 'Leave comment!' %>
<% end %>